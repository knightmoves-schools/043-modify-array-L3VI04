// Create a variable named seasons containing the four seasons
const seasons = ['Summer', 'Fall', 'Winter', 'Spring'];

// Update the second item (index 1) to be 'Autumn'
seasons[1] = 'Autumn';

// Assign the innerHTML of the element with id "result" to the second element in the seasons array
document.getElementById('result').innerHTML = seasons[1];

